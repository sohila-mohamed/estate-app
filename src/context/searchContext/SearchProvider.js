/*import React, { createContext, useContext, useReducer, useEffect } from "react";
import properties from "../../js/data";
export const SearchContext = createContext();

export const SearchProvider = ({ searchReducer, initialSearchState, children }) => {

  return (
  <SearchContext.Provider value={useReducer(searchReducer, initialSearchState)}>
    {children}
  </SearchContext.Provider>
  );
};

export const useSearchValue = () => useContext(SearchContext);*/
